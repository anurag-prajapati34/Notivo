export const TopBar = () => {
    return (
        <header className="fixed top-0 left-0 right-0 z-50 h-12 bg-gray-900 text-white flex items-center px-4 shadow">
            TopBar
        </header>
    );
};